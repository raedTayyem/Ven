{
  "scripts": {
    "start" : "cd back-end && node server.js",
    "dev": "concurrently \"cd back-end && npm run start server:dev\" \"cd front-end && npm start\" ",
    "heroku-postbuild": "cd front-end && npm install && npm run build"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "nodemon": "^2.0.4"
  }
}
